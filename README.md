# 🛡️ Бот «Страж» — ИИ-помощник по юридической проверке рекламы

**Бот «Страж»** — это интеллектуальный Telegram-бот, который помогает пользователям проверять рекламные тексты, изображения с текстом и PDF-документы на соответствие требованиям законодательства Российской Федерации, включая:

- Федеральный закон №38-ФЗ «О рекламе»  
- Уголовный, Гражданский и Кодекс об административных правонарушениях (УК РФ, ГК РФ, КоАП РФ)  
- Конституцию РФ  
- Специализированные нормативные акты (о персональных данных, СМИ, защите прав потребителей и др.)

Бот автоматически выявляет потенциально незаконные формулировки, предлагает классификацию (`законно` / `незаконно` / `требуется консультация`) и при необходимости направляет запрос на ручную проверку экспертами.

---

## ✨ Возможности

- 🤖 Анализ текста, изображений и PDF с помощью ИИ (RAG + LLM)
- 📚 Интеграция с базой юридических норм и судебной практики
- 🔗 Автоматическая подстановка ссылок на соответствующие статьи законов
- 👤 Поддержка пользовательских аккаунтов и истории запросов
- 👮‍♂️ Админка для модерации сомнительных запросов
- 🧠 Возможность дообучения и расширения базы знаний

---

## 📦 Технологический стек

- **Backend (бот)**: Python 3.12, [aiogram 3.x](https://docs.aiogram.dev/)
- **OCR**: Tesseract (русский + английский), PyPDF2
- **AI/ML**: LangChain, ChromaDB, HuggingFace Embeddings, Ollama (Mistral)
- **База данных**: PostgreSQL + SQLAlchemy (асинхронный драйвер asyncpg)
- **Контейнеризация**: Docker & docker-compose
- **Интерфейс**: Telegram Bot API

---

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**

   ```bash
   git clone https://github.com/your-username/bot_strazh.git
   cd bot_strazh
   ```

2. **Создайте и заполните `.env` файлы**

   - `bot/.env` — Telegram-токен, настройки БД и URL RAG-сервера:
     ```env
     BOT_TOKEN=<your_token>
     DATABASE_URL=<your_url>
     RAG_API_URL=<your_url>
     ADMIN_ID=<your_id>
     ```

3. **Соберите и запустите через Docker Compose**

   ```bash
   docker-compose up --build
   ```

4. **Инициализируйте суперадмина (опционально)**

   ```bash
   docker exec -it bot_strazh-bot-1 python add_admin.py
   ```

5. **Используйте бота в Telegram**

   Найдите своего бота по `@username` или отправьте `/start`.

---

## 🧠 Как работает ИИ-анализ

1. Пользователь отправляет текст/изображение/PDF.
2. Система извлекает текст (OCR + PDF-parsing).
3. Текст поступает в **RAG-сервер**, который:
   - Извлекает релевантные фрагменты из базы юридических документов (ChromaDB)
   - Передаёт их LLM (Mistral через Ollama) с системным промптом-экспертом
4. Полученный ответ парсится, определяется класс (`законно`/`незаконно`/`требуется консультация`)
5. При низкой уверенности — запрос помечается как **подозрительный** и отправляется на ручную модерацию.

---

## 📂 Структура проекта

```
.
├── AI_RAG_Server/       # FastAPI-сервер для RAG-анализа
├── bot/                 # Telegram-бот (aiogram)
├── docker-compose.yml   # Оркестрация: PostgreSQL + RAG + Bot
└── README.md
```

---

## ⚠️ Важно

> Ответы, генерируемые ИИ, **носят исключительно информационный характер** и **не являются официальным юридическим заключением**. Для принятия решений рекомендуется консультация с квалифицированным юристом.

---

## 🤝 Разработка

Проект разработан с упором на юридическую точность, безопасность данных и масштабируемость. Все конфиденциальные параметры передаются через переменные окружения, а архитектура позволяет легко заменять LLM или расширять базу знаний.

---


---

> 🛡️ **Бот «Страж»** — ваш надёжный помо
